require 'spec_helper'

describe MtgCardArt do
  it 'has a version number' do
    expect(MtgCardArt::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
